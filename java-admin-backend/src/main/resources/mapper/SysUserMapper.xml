<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.admin.repository.SysUserRepository">

    <!-- 根据用户名查询用户 -->
    <select id="findByUsername" parameterType="String" resultType="com.example.admin.entity.SysUser">
        SELECT * FROM sys_user WHERE username = #{username}
    </select>

    <!-- 检查用户名是否存在 -->
    <select id="existsByUsername" parameterType="String" resultType="boolean">
        SELECT COUNT(*) > 0 FROM sys_user WHERE username = #{username}
    </select>

</mapper>